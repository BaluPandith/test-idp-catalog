apiVersion: backstage.io/v1alpha1
kind: Component
spec:
  system: AAP
  lifecycle: production
  type: service
metadata:
  name: my-service
  description: A test service with a custom annotation.
  annotations:
    mycompany.com/owner-info: team-alpha
spec:
  type: service
  owner: user:guest
  lifecycle: production
